# 负收入逻辑全系统一致性处理 PRD 审核报告（v4）

> 评审日期：2026-06-05（第三次调整版 / v4）
> 评审对象：`docs/评审/01-prd.md`（源自 `E:/LG_DOCS/LG/docs/负收入逻辑全系统一致性处理_需求文档.md`，v4，Sprint 111）
> 已加载项目上下文：CLAUDE.md（根 + CIOaas-api）、`~/.claude/standards/common.md`、project-conventions；代码已重新复核：`MetricExtractor.java`（L162-198）、`FinancialGrowthRateServiceImpl.java`（L106/163-206）、`ColleagueCompanyServiceImpl.java`（L760-790、L920-965）、`ArrTierEnum.java`；README 未找到

---

## 〇、v4 相对 v3 的变化（已逐行 diff + 代码复核确认）

| # | 变化 | 评估 |
|---|------|------|
| D1 | §1 标题 "ARR 增长率公式 —— 单公司视图" → "—— **Benchmarking**单公司视图"；§一/§3 "基于收入的预测" → "预测时的收入基础" | ✅ **关键进展，解决历史 P0「§1 落点歧义」**：§1 标题加 "Benchmarking" 限定符，将 §1 收口到 benchmark 域的 `ARR_GROWTH_RATE` 指标（`MetricExtractor` L174-186，基于 `normalization.arr`），与基于 gross-revenue 的 `financial_growth_rate` 引擎（`FinancialGrowthRateServiceImpl`，L106 负月预过滤 + L194-201 CAGR 桥接）划清边界。"引擎①绝对值 vs 预过滤互斥" 的 P0 随之消解（详见 §三 #2/#3 状态变更） |
| D2 | §2 首条新增 "（该条件运行顺序由开发自定义）" | ⚠️ **部分回应历史 P0「剔除 × Peer Fallback 顺序」**：产品决策 = 顺序下放开发。但措辞把"顺序"等同于"纯实现细节"，**与代码事实不符**：剔除发生在 `<3` 判定之前 vs 之后会产生不同用户可见结果（先剔除→更易触发 fallback；后剔除→fallback 池更小），且现有 fallback（L927 `matchedPeerIds.size()<3`）是**公司级整体判定**、无逐期次计数，而剔除是**期次级**——"运行顺序"是否涵盖该粒度问题不确定。见 #1（降级 P1，未清零） |
| D3 | §2.5 Actual 条款末尾补 "，继续执行剔除"（v3 仅 Forecast 侧有） | ✅ **解决 v3 #14/T6 不对称问题**：Actual / Forecast 两侧回退后剔除语义现已对称 |
| D4 | §2.5 数据质量维度（第 5 点）内部**新增第三 bullet** "**ARR_t-1 < 0** 的公司（期次数据点），从同行池中**完全排除**" | ⚠️ **新引入重复 + 粒度矛盾**：(a) 与 §2 首条规则**逐字重复**（应一处定义、他处引用，违反 project-conventions）；(b) 结构矛盾——§2 首条说该规则"在现有匹配规则**基础上新增**"，此处又被塞进六维度之一的"数据质量"内部；(c) **实质风险**：数据质量维度是**公司级有效性判定**（是否有效数据公司），而该剔除是**期次级数据点剔除**，放此处易诱导开发实现成**公司级永久排除**，与 §2 "动态判定：不是永久踢出" 直接冲突。建议删除此重复 bullet，单一出处保留在 §2 首条。见 #15（新增 P1） |
| D5 | "现有同行匹配规则" → "**补充：同行匹配规则**" | ℹ️ 中性措辞变化，无语义影响 |

**v4 净结论**：取得**一项决定性 P0 关闭**——D1 用 "Benchmarking" 限定符将 §1 收口到 benchmark `ARR_GROWTH_RATE` 引擎，连带消解"引擎归属（C1）"与"绝对值 vs 负月预过滤互斥（C2）"两项历史 P0。D2/D3 各自部分缓解一项历史问题（剔除×fallback 降为 P1；不对称问题清零）。但 **D4 新增一处重复 + 粒度矛盾（新 P1）**。综合：**P0 已清零（条件性）**，由 ❌ 阻塞上调为 **⚠️ 有条件通过**——可进入 gen-tdd，但带一份待确认短清单。

---

## 一、评分仪表盘

| 维度 | 评分 | 说明 |
|------|------|------|
| 语义与表述 | 6/10 | §1 "Benchmarking" 限定符消除落点歧义（+1）；但 "FE中ARR" 含糊术语仍在（#7），§4 MRR/ARR 口径定义仍缺（#5） |
| 功能完整性 | 6/10 | §1 收口、Actual/Forecast 剔除对称（+1）；但 §4 Forecast 路径负值排除缺口（#4）、§3 全负兜底（#6）仍缺 |
| 操作闭环 | 5/10 | "顺序下放开发"部分缓解（+1），但期次级剔除 × 公司级 fallback 粒度终态仍未定义（#1）；§3/§4 24 月全负兜底无终态（#6） |
| 数据一致性 | 7/10 | §1 已明确约束 benchmark `ARR_GROWTH_RATE` 引擎（C1/C2 关闭，+1）；$20M 边界与 `ArrTierEnum`/`calculateArrRangeForPeerFilter` 一致 |
| 权限覆盖 | N/A | 纯计算逻辑变更，无角色/权限维度（不计入综合） |
| 业务规则 | 5/10 | §1 引擎归属已定（+1）；但仍无集中 BR 编号；D4 新增"数据质量含期次剔除"与"动态判定"冲突（#15） |
| 编号与可追溯性 | 2/10 | 全文仍无 SC/BR/AC 编号，无验收标准小节，无 §七 BR 集中定义 |
| **综合** | **⚠️ 有条件通过** | 历史 4 项 P0 已清零（引擎归属/互斥经 D1 消解、剔除×fallback 经 D2 降级 P1、Forecast 缺口降级、不对称经 D3 清零）。剩余均为 P1/P2，不阻塞 gen-tdd，但需在 TDD 阶段回填决策 |

---

## 二、评审通过项

- ✅ 业务背景清晰：明确两类问题（个体公司指标 / 基准与同行计算），目标可理解。
- ✅ §1 ARR 增长率三分支表（>0 / =0或null / <0）逻辑自洽，含正反例。
- ✅ §3 收入基础回溯规则（24 个日历月、首个非负值）描述具体、可执行。
- ✅ §4 Target Company 排除示例（三个月 gross revenue 取均值）量化清晰，可直接转测试用例。
- ✅ §2 动态判定说明（按期间剔除而非永久踢出）消除了一处潜在歧义。
- ✅ §2.4 ARR 分桶 `[$5M,$20M)` / `[$20M,+∞)` 与代码 `ArrTierEnum.fromArr`、`calculateArrRangeForPeerFilter`（L770-778）完全一致。
- ✅（**v4 新解决**）§1 标题加 "Benchmarking" 限定符，将 §1 收口到 benchmark `ARR_GROWTH_RATE` 指标（`MetricExtractor` L174-186），与 gross-revenue `financial_growth_rate` 引擎划清边界——**关闭历史 P0「§1 落点歧义」及连带的「绝对值 vs 负月预过滤互斥」**。
- ✅（**v4 新解决**）§2.5 Actual 侧补"继续执行剔除"，Actual/Forecast 两侧回退后剔除语义对称（关闭 v3 #14/T6）。

---

## 三、问题与建议

| # | 问题分类 | 问题描述 | 严重程度 | 是否阻塞 | 修订类别 | 状态 |
|---|----------|----------|----------|----------|----------|------|
| 1 | 操作闭环 | §2 "运行顺序由开发自定义" 把期次级剔除 × 公司级 Peer Fallback（`<3`）的交互当作纯实现细节下放，但二者**不等价于实现细节**：先剔除→更易触发 fallback；后剔除→fallback 池更小，用户可见结果不同。且代码 fallback（L927 `matchedPeerIds.size()<3`）为**公司级整体判定**，无逐期次计数；"某期次剔除致 <3 是否触发该期次回退""回退到全平台再剔除仍 <3 的终态"未被"运行顺序"覆盖 | ~~P0~~ → **P1** | 不阻塞 gen-tdd（已下放），但需 TDD 回填 | 需用户决策 | **v4 由 D2 降级**：要求开发选定方案后回填 PRD/TDD 并在验收用例固化 |
| 2 | 业务规则 | 系统两套增长率引擎，§1 约束哪套——v3 为 P0 阻塞 | ~~P0~~ **已解决** | 否 | — | **v4 由 D1 解决**：§1 标题 "Benchmarking单公司视图" 收口到 benchmark `ARR_GROWTH_RATE`（`MetricExtractor` L174-186） |
| 3 | 业务规则 | §1 "负 t-1 取绝对值" 与引擎① "负月预过滤 + CAGR 桥接"（L106/164/172）互斥——v3 为 P0 阻塞 | ~~P0~~ **已解决** | 否 | — | **v4 由 D1 消解**：§1 既限定 benchmark 引擎②，引擎①不在 §1 范围。**遗留 P2 建议**：PRD 宜明示引擎①"跳过负月 + 复合桥接"为**有意保留/豁免**，否则开发做 §3 时仍可能误改引擎① → 见 #16 |
| 4 | 功能完整性 | §4 "适用于 Benchmarking 和 System Generated Forecast Generation"，但 Forecast 路径 `getForecastArrForBenchmark`（L956-965）用 `normalization.arr` 或 `revenue×12`，**无负值排除**；负值排除目前仅在 Actuals 路径（上游 L106 已过滤）事实生效 | ~~P0~~ → **P1** | 不阻塞 gen-tdd | 需用户决策 | **v4 降级**：§1 收口后此项不再卡住整体；但 Forecast 路径缺口仍需在 TDD 明确（normalization 表是否已过滤负月待确认） |
| 5 | 语义与表述 | §4 仍缺 ARR=MRR×12 与三种 recognition（Last Month / Last Three / TTM，分母 = `subList.size()`，对齐代码 L780-789）的正式定义，仅靠单一示例支撑 | P1 | 不阻塞 | 需用户决策 | **v3 遗留，v4 未恢复**：建议在 §4 补"术语与口径"块 |
| 6 | 操作闭环 | §3 步骤 3「回溯 24 个月仍无非负值」的兜底未定义（不生成预测 / 置 0 / 沿用现状？）；§4 recognition=1（Last Month）基准月被排除/缺失时 L784 返回 `ZERO×12=0`→TIER_0 无回溯；窗口全负时 `calculateArrRangeForPeerFilter(null/负)→0`（L771/777）落 TIER_0，均未在 PRD 写明 | P1 | 不阻塞 | 需用户决策 | **v3 遗留，v4 未解决** |
| 7 | 语义与表述 | §4 "该ARR的计算与当前FE中ARR的计算不同" 中 "FE中ARR" 为实现侧含糊术语，读者无法唯一推断指代 | P1 | 不阻塞 | 需用户决策 | **v3 遗留，v4 未解决** |
| 8 | 编号与可追溯性 | 全文仍无 SC/BR/AC 编号，无独立「验收标准」小节，BR 未集中定义（违反 project-conventions §2） | P1 | 不阻塞 | 可自动修订 | **v3 遗留，v4 未解决** |
| 9 | 语义与表述 | §2「不影响该公司自身 ARR 增长率的展示」与 §4「不影响公司在常规视图中的 ARR 展示」表达同一原则，分散重复 | P2 | 非阻塞 | 可自动修订 | **遗留**，未合并 |
| 10 | 边界条件 | 货币/单位未说明：ARR 桶用美元，系统多币种；负收入判定与分桶是否在归一化货币（USD）下进行未提及 | P2 | 非阻塞 | 需用户决策 | **遗留**，未解决 |
| 11 | 数据一致性 | 目标公司自身 t-1<0 时（§1 绝对值公式语义 ≠ 同行常规公式）其增长率落入"已剔除负值同行"分布求百分位，可比性未说明 | P2 | 非阻塞 | 需用户决策 | **遗留**，未解决 |
| 12 | 业务规则 | 手动同行组（`getManualColleagueGroupCompanyIdsIfEligible`，现有逻辑 ≥4 家时优先于系统匹配）是否同样执行 t-1<0 期次剔除未说明；§2 适用范围只列 internal LG-derived 与 portfolio-level | P2 | 非阻塞 | 需用户决策 | **遗留**，未解决 |
| 13 | 非功能需求 | 三个 benchmark 定时业务仅 SQS 触发（项目记忆 `project_benchmark_sqs_only_trigger`）。规则变更后历史 growth rate / benchmark 是否重算、由哪条 SQS 链触发未提及；无历史回算方案；无前端需求（剔除导致同行数变化是否提示用户等） | P3 | 非阻塞 | 需用户决策 | **遗留**，未解决 |
| 14 | 数据一致性 | §1「ARR_t-1=0 或 null → 增长率 0」：t-1=0 的数据点增长率被人为置 0 后仍进入同行分布，存在零扭曲；确认是有意保留还是遗漏 | P2 | 非阻塞 | 需用户决策 | **v3 遗留，v4 未回应** |
| 15 | 业务规则 | **v4 新增**：§2.5 数据质量维度内新增 bullet "ARR_t-1<0 完全排除" 与 §2 首条逐字重复；且把**期次级剔除**塞进**公司级有效性判定**维度，易诱导开发实现成公司级永久排除，与 §2 "动态判定：不是永久踢出" 冲突 | P1 | 不阻塞 | 可自动修订 | **v4 新增**：删除该重复 bullet，单一出处保留 §2 首条 |
| 16 | 业务规则 | **v4 新增建议**：§1 经 D1 收口到 benchmark 引擎②后，引擎①（`financial_growth_rate`，"跳过负月 + CAGR 桥接"）落入 §3 改造范围之外。PRD 未明示引擎① 为有意保留/豁免，开发做 §3「预测收入基础」时仍可能误改引擎① | P2 | 非阻塞 | 需用户决策 | **v4 新增**：在 §3 或术语块明示引擎① 现策略豁免、不在本需求改动范围 |

修订类别：**可自动修订** | **需用户决策**

### 表述与歧义问题

| # | 位置（章节/段落） | 原文摘录 | 问题类型 | 建议改写 |
|---|------------------|----------|----------|----------|
| T1 | §4 | "该ARR的计算与当前FE中ARR的计算不同，不应相互影响" | 歧义 | "用于同行框定的 ARR（peer-filter ARR）与公司常规视图/详情页展示的 ARR 计算口径不同，二者互不影响"，各自标注来源（peer-filter ARR = `calculateGrowthRateArrByDateForPeerFilter`；展示 ARR = `normalization.arr`） |
| T2 | §2.5 数据质量第 5 点 / 第三 bullet | "**ARR_t-1 < 0** 的公司（期次数据点），从同行池中**完全排除**" | 重复啰嗦 + 前后不一致 | **删除该 bullet**。剔除规则单一出处保留在 §2 首条；此处仅"连续六个月非负 gross revenue → 有效数据公司"为公司级判定，不应混入期次级剔除 |
| T3 | §2 首条 | "（该条件运行顺序由开发自定义）" | 不闭环（被误标为实现细节） | 改为"剔除与『有效同行 <3 回退』的执行顺序由开发选定，但须在 TDD 固化并满足：①期次级剔除后若该期次有效同行 <3，{触发/不触发}该期次 fallback；②回退全平台再剔除仍 <3 的终态为 {…}" |
| T4 | §4 示例 | "用历史三个月 gross revenue(MRR) 计算 ARR" | 理解缺口 | 补三种 recognition（Last Month / Last Three Months / TTM）MRR 定义与"可用 = 非负（≥0）且非空"定义，分母 = 窗口内可用月份数，对齐代码 L780-789 |
| T5 | §3 步骤 3 | "在回溯窗口内找到第一个非负的 Gross Revenue 值" | 不闭环 | 补一句："若 24 个月内均为负，则 {兜底：不生成预测 / 置 0 / 沿用现状 / 落 TIER_0}" |
| T6 | §1 标题 / §3 | "Benchmarking单公司视图" / "预测时的收入基础" | 详略失当（建议补） | §1 收口正确；建议在 §3 加一句明示：引擎① `financial_growth_rate` 的"跳过负月 + CAGR 复合桥接"为现状有意保留，不在本需求改动范围（防误改） |

**问题类型枚举**：歧义｜不闭环｜理解缺口｜前后不一致｜套话/水分｜重复啰嗦｜详略失当

---

## 三、待确认事项

| # | 问题 | 需要谁确认 | 是否阻塞后续设计 |
|---|------|-----------|----------------|
| C1 | 期次级剔除与公司级 Peer Fallback（`<3` 回退）的执行顺序与终态：某期次剔除致 <3 是否触发该期次回退；回退全平台再剔除仍 <3 的终态；开发选定后须回填 TDD + 验收用例 | 业务 + 后端 | **不阻塞 gen-tdd**（已下放），需 TDD 阶段固化 |
| C2 | §4 Forecast 路径（`getForecastArrForBenchmark` L956-965）如何排除负 gross revenue；normalization 表是否已过滤负月 | 后端 | 不阻塞，TDD 明确 |
| C3 | §4 ARR/MRR 口径是否补正式定义；"可用月份数"精确定义（非负？非空？两者？）；§1 benchmark ARR 与 peer-filter ARR 是否同源 | 业务 | 不阻塞 |
| C4 | "FE中ARR" 具体指代（常规视图展示 ARR？哪段计算？） | 产品 + 前端 | 不阻塞 |
| C5 | §3「回溯 24 个月全负」兜底；Last Month（recognition=1）基准月被排除/缺失时取值（回溯 / 置 0 / TIER_0）；窗口全负 ARR 无法计算的落档 | 业务 | 不阻塞 |
| C6 | 引擎①（`financial_growth_rate`，跳过负月 + CAGR 桥接）是否为有意保留/豁免、不在本需求改动范围（防 §3 误改） | 业务 + 后端 | 不阻塞 |
| C7 | 手动同行组（≥4 家时优先）是否同样执行 t-1<0 期次剔除 | 业务 | 非阻塞 |
| C8 | 目标公司自身 t-1<0 时其增长率百分位可比性；t-1=0 置 0 数据点的零扭曲是否有意保留 | 业务 | 非阻塞 |
| C9 | 正负判定与 ARR 分桶的币种基准（是否归一化 USD）；历史数据重算与 SQS 触发时机 | 业务 + 后端 | 非阻塞 |

---

## 四、假设（待验证）

- §1「Benchmarking单公司视图」既加 "Benchmarking" 限定符，则 §1 约束 benchmark 引擎② `ARR_GROWTH_RATE`（`MetricExtractor` L174-186，基于 `normalization.arr`，prev null/0→0，待加 <0 绝对值分支），不约束 gross-revenue 引擎① `financial_growth_rate`。**已基本确认，待 C6 收尾**。
- §4 peer-filter ARR 即 `calculateGrowthRateArrByDateForPeerFilter`（L780-789，窗口求均 ×12，分母 = `subList.size()`），"可用月份数" = 窗口内非负行数（因上游 L106 过滤已等价）。
- §2.5 数据质量与回退规则为现有规则，本需求仅"新增 t-1<0 期次剔除"；D4 新增 bullet 为重复声明、应删除而非新逻辑。
- Peer Fallback 文案沿用现有 `FALLBACK_MSG`（L830），本需求不新增展示文案。
- 本需求为纯后端计算逻辑变更，无新增前端页面/角色权限。

---

## 五、建议补充到 PRD 的内容

- **术语与口径小节**（新增，置于 §二之前）：明确 ARR = MRR×12、三种 recognition MRR 定义（分母 = 可用月份数）、"可用"定义、peer-filter ARR 与展示 ARR（"FE中ARR"）各自来源、data type 枚举（Actuals / Committed forecast / System generated forecast / Internal Peer）、§1 已限定的 benchmark `ARR_GROWTH_RATE` 引擎、引擎① 豁免声明。
- **编号体系**：为功能点分配 SC-01~SC-04，散落规则集中为 §七 BR-01~BR-nn，每 SC 补可判定 AC。
- **删除重复**：删除 §2.5 数据质量维度内的 "ARR_t-1<0 完全排除" bullet（与 §2 首条重复且粒度矛盾）。
- **验收标准**：至少覆盖 $20M 边界归桶、负 t-1 取绝对值两示例、24 个月回溯命中/未命中兜底、剔除致 <3 触发 fallback（期次粒度）、Forecast 路径负值排除、Last Month 基准月缺失行为、自身展示不受剔除影响。
- **非功能需求**：历史数据重算策略与 SQS 触发；多币种归一化基准；前端是否提示同行数变化。

---

## 六、已自动修订项（流水线调用时填写）

| # | 位置 | 修订内容 | 类型 |
|---|------|----------|------|
| 1 | `docs/评审/01-prd.md` | 同步为外部 PRD v4 内容（§1 标题加 "Benchmarking"；§一/§3 "基于收入的预测"→"预测时的收入基础"；§2 首条加"（该条件运行顺序由开发自定义）"；§2.5 Actual 加"继续执行剔除"+ 数据质量维度新增 ARR_t-1<0 bullet；"现有同行匹配规则"→"补充：同行匹配规则"） | 状态补充（同步上游） |
| — | 正文 | 未对 PRD 正文做语义级自动修订：剩余项（剔除×fallback 终态 C1、Forecast 缺口 C2、MRR 口径 C3、FE中ARR C4、24月兜底 C5、引擎①豁免 C6）均涉及业务/技术决策，建议 TDD 阶段回填；可自动修订项（删除 §2.5 重复 bullet #15、合并 #9 重复表述、补编号 #8）留待用户确认后机械修正 | — |

---

## 结论

**⚠️ 有条件通过（P0 已清零）**。v4 取得**决定性进展**：§1 标题加 "Benchmarking" 限定符（D1），将 §1 收口到 benchmark `ARR_GROWTH_RATE` 引擎（`MetricExtractor` L174-186），**一举关闭历史两项 P0**——「§1 约束哪套增长率引擎」与「绝对值分母 vs 引擎①负月预过滤互斥」。配合 D2（剔除×fallback 顺序下放，降为 P1）、D3（Actual/Forecast 剔除对称，清零 v3 #14），**v3 的 4 项历史 P0 全部清零**。

代价是 D4 引入一处**重复 + 粒度矛盾的新 P1**（§2.5 数据质量维度混入期次级剔除，与"动态判定"冲突）——但属可机械修订，不阻塞。

**解除阻塞结论**：可进入 gen-tdd。**进入 gen-tdd 前的待确认短清单（建议在 TDD 阶段固化，非阻塞）**：
1. **C1**：期次级剔除 × 公司级 Peer Fallback 的顺序与终态（开发选定后回填 TDD + 验收用例）。
2. **C2**：§4 Forecast 路径 `getForecastArrForBenchmark`（L956-965）负值排除缺口。
3. **C6**：引擎①（`financial_growth_rate`，跳过负月 + CAGR 桥接）确认为有意保留/豁免，防 §3 误改。
4. **#15（可自动修订）**：删除 §2.5 数据质量维度内重复的 "ARR_t-1<0 完全排除" bullet。
5. **C3/C4/C5**：§4 MRR/ARR 口径定义、"FE中ARR" 指代、24 月回溯全负兜底——TDD 阶段补全即可。

以下代码核对结论本次已重新复核并继续有效：
- 两套增长率引擎并存（引擎① `financial_growth_rate`，L106 负月整行预过滤 + L194-201 CAGR 跨月桥接；引擎② `MetricExtractor.ARR_GROWTH_RATE` L176-186，prev null/0→0，**无负值分支**，待加 §1 绝对值分支）。
- §2.4 $20M 边界与 `ArrTierEnum`、`calculateArrRangeForPeerFilter`（L770-778，`>=B20000→5`）一致。
- §4 Forecast 路径 `getForecastArrForBenchmark`（L956-965）用 `normalization.arr` 或 `revenue×12`，**无负值排除**（真正缺口）。
- Peer Fallback（L927 `matchedPeerIds.size()<3`）为公司级整体判定，无逐期次计数——印证 #1 粒度问题。
- recognition=1（Last Month）基准月缺失 → L784 `ZERO×12=0`→TIER_0 无回溯；窗口全负 → `calculateArrRangeForPeerFilter(null/负)→0`（L771/777）落 TIER_0。
